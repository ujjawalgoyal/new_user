//= ========================= Load Modules Start =======================

const _ = require("lodash");

//= ========================= Load internal modules ========================================

const profileService = require("./profileService");
const appConstants = require("./profileConstants");
const appUtils = require("../appUtils");
const jwtHandler = require("../jwtHandler");
const exceptions = require("../customExceptions");

//= ========================= Load Modules End ==============================================

// Method to create places
const createUserDocument = async (params) => {
    try {
        const resp = await profileService.createUserDocument(params);
        return resp;
    } catch (err) {
        throw err;
    }
};
//= ==================================== Export module =======================================

module.exports = {
    createUserDocument
};

//= ======================================= End =============================================
