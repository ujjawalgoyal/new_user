//= ========================= Load Modules Start =======================

const _ = require("lodash");

//= ========================= Load internal modules ========================================

const placesService = require("./placesService");
const appConstants = require("./placesConstants");
const appUtils = require("../appUtils");
const jwtHandler = require("../jwtHandler");
const exceptions = require("../customExceptions");

//= ========================= Load Modules End ==============================================

// Method to create places
const createPlaces = async (params) => {
    try {
        const resp = await placesService.createPlaces(params);
        return appConstants.MESSAGES.passwordChanged;
    } catch (err) {
        throw err;
    }
};
//= ==================================== Export module =======================================

module.exports = {
    createPlaces
};

//= ======================================= End =============================================
